# Miss You Landing Page

Open `index.html` locally to preview the site.

Files:
- `index.html` — page structure and placeholder text
- `style.css` — all styling
- `script.js` — scroll reveal and button interaction
- `assets/` — the three photos

Built as a static site so it can be hosted free on GitHub Pages.
